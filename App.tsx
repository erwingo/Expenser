import * as React from 'react';
import { StyleSheet, Text, View, SectionList, StatusBar } from 'react-native';

import Home from './src/_components/Home'

export default class App extends React.Component {
  render() {
    return (
      <Home />
    );
  }
}
